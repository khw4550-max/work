# '26년 AI Advanced 과정 · 입과 오리엔테이션 슬라이드

발표용 슬라이드 덱입니다. 빌드 과정 없이 브라우저에서 바로 열립니다.

```
index.html          전체 31장 + 이미지까지 들어있는 완전 단독 파일
assets/             원본 보관용 (index.html은 참조하지 않음)
  evacuation.jpg    22p 비상시 대피장소 안내도 — HTML에 base64로 삽입되어 있음
                    11p 평가 Report 예시 · 4p 진행자 사진도 base64로 내장 (원본 미보관)
  humancenter.png   현재 미사용 (휴먼센터 전경 — 해당 장표 삭제됨, 보관용)
```

## 여는 방법

`index.html`을 브라우저로 열면 됩니다. 서버나 설치 과정이 필요 없습니다.
**이 파일 하나만 있으면 됩니다** — 이미지가 HTML 안에 base64로 들어 있어 `assets/` 폴더 없이도 그대로 보입니다.
메일로 보내거나 USB로 옮길 때도 `index.html`만 챙기면 됩니다.

| 조작 | 방법 |
|---|---|
| 다음 장 | **화면 아무 곳이나 클릭** · **휠 아래로** · `→` `↓` `Space` `Enter` `PageDown` |
| 이전 장 | **화면 우클릭** · **휠 위로** · `←` `↑` `Backspace` `PageUp` |
| 원하는 장표로 이동 | 하단 바의 **`11 / 31 · 평가 기준`을 클릭** (또는 `G`) → 목록에서 고르거나 번호·이름 입력 후 Enter |
| 처음 / 마지막 | `Home` / `End` |
| 전체화면 | `F` |
| 발표자 노트 | `N` |
| 목록 닫기 | `Esc` |

**전체화면에서는 하단 컨트롤바가 숨겨집니다.** 발표 중 마우스가 조금 움직여도
바가 떴다 사라지지 않도록 한 것입니다. 필요할 때는 **커서를 화면 맨 아래(하단 80px)로
내리면** 잠깐 나타나고, 2.5초 뒤 다시 사라집니다. `G`로 페이지 목록을 여는 것은
전체화면에서도 그대로 됩니다.

휠은 관성 스크롤로 여러 장이 넘어가지 않도록 420ms 간격으로 제한해 두었습니다.

### 프레젠터 리모컨(빔 포인터)

USB 리모컨은 키보드로 인식되어 따로 설정 없이 동작합니다. 버튼별 동작:

| 리모컨 버튼 | 보내는 키 | 이 덱에서 |
|---|---|---|
| 다음 / 이전 | `PageDown` / `PageUp` | 한 장씩 이동 |
| 레이저·클릭 모드 | 마우스 좌클릭 | 다음 장 |
| 발표 시작 | `F5` | **전체화면 진입** (새로고침 차단 처리함) |
| 발표 종료 | `Esc` | 전체화면 해제 |
| 화면 끄기 | `B` / `.` | 동작 없음 |

`F5`는 원래 브라우저 새로고침이라 발표 중 눌리면 화면이 끊깁니다. 기본 동작을
막고 전체화면 진입으로 돌려두었습니다. `Esc`로 전체화면이 풀리는 것은 브라우저가
강제하는 동작이라 막을 수 없습니다 — 슬라이드 위치는 유지되므로 `F` 로 다시
전체화면으로 들어가면 됩니다.

폰트(Pretendard)는 CDN에서 불러옵니다. 오프라인이면 시스템 한글 폰트로 대체되어
보이긴 하지만 자간·굵기가 조금 달라집니다.

1p 부제의 손글씨체(`HandKR` = Nanum Pen Script)는 **쓰이는 글자만 추려 HTML에
내장**했습니다(7KB). 네트워크가 없어도 그대로 나옵니다. 다만 서브셋이라
**이 문구의 글자만** 들어 있으니, 손글씨체로 다른 문구를 쓰려면 폰트를 다시 받아야 합니다:
`curl "https://fonts.googleapis.com/css2?family=Nanum+Pen+Script&text=<쓸글자>"` 로
woff2 주소를 얻어 base64로 교체하세요.

## 장표 수정 방법

각 장표는 `<section class="slide" data-label="...">` 한 블록입니다.
고치고 싶은 장표의 한글 라벨로 검색하면 바로 찾을 수 있습니다.

```
data-label="평가 기준"        ← 이 라벨로 검색
data-screen-label="12"        ← 0부터 시작하는 내부 순번
data-speaker-notes="..."      ← N 키로 뜨는 발표자 노트
```

작성 규칙:

- 캔버스는 **1920 × 1080 고정**이고 화면 크기에 맞춰 자동 축소됩니다.
  위치는 대부분 `position:absolute` + px 좌표입니다.
- 스타일은 인라인입니다. 공용 CSS는 `<head>`의 `.slide` 규칙뿐입니다.
- 한글 줄바꿈은 `word-break: keep-all`로 **띄어쓰기 단위로만** 끊기게 해두었습니다.
  특정 문구를 반드시 한 줄에 넣어야 하면 `white-space:nowrap` + 자간(`letter-spacing`) 조절을 씁니다.
- 카드에 `height`가 고정된 경우 글자를 늘리면 아래로 넘칠 수 있습니다.
  수정 후에는 브라우저에서 해당 장표를 눈으로 한 번 확인하세요.
- **큰 제목은 줄이 늘어나면 아래 문구를 덮습니다.** 좌표가 절대값이라 밀려나지 않기 때문입니다.
  제목을 고쳤으면 겹침 여부를 반드시 눈으로 확인하고, 한 줄로 둘 제목에는
  `white-space:nowrap`을, 두 줄로 쓸 제목에는 `<br />`을 명시하세요.
- **인라인 스타일에 `position`을 두 번 쓰지 마세요.** 뒤에 온 선언이 이깁니다.
  장식용 도형을 담으려고 `position:relative`를 덧붙이면 앞의 `position:absolute`가
  무효가 되어 좌표가 통째로 어긋납니다. `absolute` 자체가 이미 기준점이 되므로
  `relative`는 붙일 필요가 없습니다.
- 이미지를 새로 넣을 때는 반드시 **base64 data URI로 삽입**하세요.
  `assets/`를 상대경로로 참조하면 HTML만 따로 열었을 때 엑박이 뜹니다.
  변환: `python3 -c "import base64;print('data:image/jpeg;base64,'+base64.b64encode(open('파일.jpg','rb').read()).decode())"`

색상 팔레트: `#006FFF`(메인) · `#22A7F5`(1일차) · `#0050BB`(2~3일차) · `#16305E`(4일차) · `#0E1116`(어두운 장표)

## 장표 구성 (31장)

| # | 라벨 | # | 라벨 |
|---|---|---|---|
| 1 | 대기화면 | 17 | 2일차 상세 |
| 2 | 목차 | 18 | 3일차 상세 |
| 3 | 간지 01 | 19 | 4일차 상세 |
| 4 | 진행자 소개 | 20 | 간지 04 |
| 5 | 입과자 현황 | 21 | 시설 이용 안내 |
| 6 | 아이스브레이킹 | 22 | 금지사항 · 안전 |
| 7 | 간지 02 | 23 | 교육 에티켓 |
| 8 | 교육체계 4단계 | 24 | 간지 05 |
| 9 | 교육 취지 | 25 | 사내 자료 활용 원칙 |
| 10 | SLIC 인증제 | 26 | 사전 준비사항 |
| 11 | 평가 기준 | 27 | 문의처 |
| 12 | 간지 03 | 28 | Break Time |
| 13 | 3개 모듈 · 4일 흐름 | 29 | Wrap-Up 설문 |
| 14 | 학습목표 | 30 | 야간 세션 전환 |
| 15 | 일정 총괄 | 31 | 일차 종료 |
| 16 | 1일차 상세 | | |

## 남은 작업

- 없음. (퀴즈 장표는 삭제되었습니다.)

## 확정된 주요 수치 (수정 시 함께 맞출 것)

- 일정: 8.18(화) ~ 8.21(금) · 3박 4일 합숙 · 휴먼센터 217호
- 식사: 점심 12:00~13:00 / **저녁 18:00~19:00** (4층 식당 · 명찰 패용)
- 익일 착석: **8:50까지 강의장**
- 야간 세션: 1~3일차 19:00~21:00 · **자율 참여** (2~3일차는 1:1 멘토링 병행)
- 팀: 10명 × 5개 팀 (총 50명) — 유사 기술 그룹으로 편성
  1팀 사내지식 RAG · 2팀 정형 데이터 조회/검증 · 3팀 외부정보 수집/모니터링 ·
  4팀 문서 파싱/변환/생성 · 5팀 영업 브리핑/코칭
- 좌석: 앞줄 1·2·3팀, 뒷줄에 4팀(1~2팀 사이) · 5팀(2~3팀 사이)
- 평가: 5개 기준 100점 — 25 / 20 / 15 / 25 / 15점
  **80점 이상 SLIC 3 인정, 90점 이상 인사(자격)가점 1점**
  야간 미니과제는 평가에 포함되지 않음
- 플랫폼: samsunglife.elice.io · Knox ID@samsung.com + 본인 설정 패스워드
