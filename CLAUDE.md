# 교육 OT용 HTML 슬라이드 작업 메모

'26년 AI Advanced 과정 OT 덱(`index.html`)을 만들며 쌓인 것들.
**다음에 비슷한 사내 교육 OT 슬라이드를 만들 때 이 문서를 먼저 읽을 것.**
덱 자체의 사용법·수정법은 `README.md`에 있다.

---

## 1. 이 사용자가 일하는 방식

- **페이지 번호로 지시한다.** "13p 우측 인정 기준을…" 식. 그런데 장표 삭제·추가·순서 변경이
  잦아 번호가 계속 밀린다. **응답마다 갱신된 번호 지도를 표로 주고**, 다음 요청 때
  이 번호로 말해달라고 안내할 것.
- **번호가 어긋난 지시가 자주 온다.** 이전 버전 번호를 그대로 쓰는 경우가 많다.
  번호보다 **내용이 항상 정확하다.** 지시 내용과 해당 번호의 장표가 안 맞으면
  내용으로 대상을 특정하고, 작업 후 "말씀하신 N번은 현재 M번입니다"라고 알릴 것.
  번호가 안 맞는다고 되묻지 말 것 — 내용이 명확하면 그대로 진행하는 걸 선호한다.
- **여러 건을 한 번에 던진다.** 5~10개 항목이 한 메시지에 온다. 전부 처리하고,
  응답에서 항목별로 한 줄씩 결과를 정리해 준다.
- **판단을 맡기되 알려주길 원한다.** "너가 검토해줘", "이게 좋을까 저게 좋을까"를
  자주 묻는다. 근거를 들어 **하나를 추천**하고 그대로 실행할 것. 선택지만 나열하면 안 된다.
- 수정 결과물을 매번 파일로 받아 직접 열어본다. 작업 후 `index.html`을 항상 보낼 것.

## 2. 원문을 다루는 원칙

사용자가 주는 문구는 대부분 실무 메모라 다듬을 곳이 있다. **손대되 반드시 알린다.**

- **명백한 오타·맞춤법은 고친다.** 실제로 고쳤던 것들:
  `평찰 → 명찰` · `배정 된 → 배정된` · `복호화 된 → 복호화된` · `키 분출 → 키 배부` ·
  `AI로 기술을 통해 → AI 기술을 통해`. 고친 뒤 한 줄로 "이렇게 적었습니다"라고 언급.
- **시간 표기는 앞자리 0을 뺀다.** `07:00 ~ 08:00` → `7:00 ~ 8:00`.
- **모순되게 읽히는 나열은 구조로 바꾼다.** 예: "대외비 절대불가" 다음에 "예외적으로 가능"이
  오면 훑는 사람은 헷갈린다 → **원칙 → 예외** 또는 **3단 등급(절대 불가 / 조건부 / 가능)** 으로.
- **한 문장은 "이유 + 요청"으로 쪼갠다.** 본문에 이유, 하단 강조에 행동.
  ("책상이 혼잡합니다" + **"다 드신 것은 수시로 정리"**)
- **가장 중요한 항목은 하위 항목에서 꺼낸다.** 놓치면 손해가 큰 내용(예: 대외비로 만든
  Agent는 개인 계정에서 못 씀)은 별도 패널로 독립시킨다.
- **강조는 하나만.** 인정 기준에서 80점만 크게, 90점은 각주로 내렸더니 훨씬 읽혔다.
- **없는 규정을 만들지 않는다.** 원문에 없는 항목(산출물 반출 가부 등)은 지어내지 말고
  "이 부분이 빠져 있는데 참가자가 반드시 물어볼 것"이라고 짚어줄 것. 실제로 이렇게 물었더니
  사용자가 규정을 알려줬고 장표가 좋아졌다.
- **중복은 제거하되 물어본다.** 간지 제목을 바꾸면 목차의 같은 항목도 함께 바꾼다(자동).
  두 장표에 같은 내용이 겹치면 어느 쪽을 남길지 물어본다.

## 3. 레이아웃 함정 (전부 실제로 겪은 것)

이 덱은 **1920×1080 고정 캔버스 + `position:absolute` px 좌표**다. 그래서:

- **인라인 스타일에 `position`을 두 번 쓰면 뒤가 이긴다.** 장식 도형을 담으려고
  `position:relative`를 뒤에 덧붙이면 앞의 `absolute`가 죽어 요소가 화면 밖으로 나간다.
  **두 번 겪었다.** `absolute` 자체가 이미 기준점이므로 `relative`를 붙일 필요가 없다.
- **제목이 두 줄로 감기면 아래 텍스트를 덮는다.** 좌표가 절대값이라 아래가 밀려나지 않는다.
  넘침 검사로는 안 잡힌다 → **겹침 검사를 따로 돌려야 한다**(아래 4번).
  한 줄로 둘 제목엔 `white-space:nowrap`, 두 줄엔 `<br />`을 명시.
- **flex 자식에 `min-width:0`이 없으면** `nowrap` 텍스트가 컨테이너를 밀어낸다.
- **HTML은 선행 공백을 접는다.** 코드 블록 들여쓰기는 `&nbsp;`로.
- **고정 `height` 카드는 글이 늘면 넘친다.** `min-height`를 쓰거나 그리드 stretch에 맡길 것.
- **여백이 제각각이면 지적당한다.** 본문 블록과 하단 박스를 각각 절대배치하면 장표마다
  간격이 달라진다(31 / 4 / 14px였다). **하나의 flex 컬럼으로 묶고 `gap`으로 통일**할 것.
- **한글은 `word-break:keep-all`.** 중점(·)이 줄 끝에 매달리면 `·&nbsp;뒷말`로 묶는다.
- **이미지는 반드시 base64 data URI로 내장.** 상대경로(`assets/…`)로 두면 HTML만
  단독으로 열었을 때 엑박이 뜬다. 사용자는 파일 하나만 들고 다닌다 — 실제로 이걸로 한 번 깨졌다.
  PDF는 `pypdfium2`로 렌더 → 표시 크기의 2배 해상도로 축소 → JPEG 84 품질.
- **인물 사진은 자르지 말고 배경색으로 채워 정사각으로.** 정사각 크롭했더니 얼굴만 꽉 찼다.
  모서리 색을 샘플링해 좌우를 패딩하면 원래 구도가 유지된다.

## 4. 매 수정마다 돌릴 검사 (Playwright)

`/opt/node22/lib/node_modules/playwright`를 쓴다(로컬 설치 금지 — 번들 브라우저와 버전이 어긋난다).

1. **넘침 검사** — 각 장표의 모든 요소가 1920×1080 밖으로 나가는지. 텍스트 없는 장식
   요소는 제외(의도적으로 잘라내는 도형이 있다).
2. **겹침 검사 — 두 종류를 다 돌려야 한다.**
   - *텍스트 겹침*: 텍스트를 가진 잎 노드끼리 바운딩 박스가 겹치는지.
     **이 검사로 원본부터 있던 버그 2건을 찾았다.**
   - *카드 박스 겹침*: 배경·테두리를 가진 요소(카드/패널)끼리 겹치는지.
     텍스트 검사만으로는 **카드가 겹쳐도 글자만 안 겹치면 통과해 버린다** — 실제로
     표 카드 위에 강조 띠가 14px 파고든 걸 놓쳤다. 칩·배지(140×60 미만),
     `clip-path` 장식, 전면 배경은 제외할 것.
3. **스크린샷으로 눈 확인** — 수치가 통과해도 여백 균형은 눈으로 봐야 한다.
4. 내비게이션을 손댔으면 **동작 테스트**(클릭/휠/키/스와이프/전체화면)까지.

전부 통과한 뒤에 커밋하고, 응답에 "넘침 0건 · 겹침 0건"을 적어준다.

## 5. 발표 환경에서 실제로 문제되는 것

- **프레젠터 리모컨**은 `PageUp`/`PageDown`/방향키를 보낸다. `F5`(발표 시작)도 흔한데
  브라우저에선 새로고침이라 발표가 끊긴다 → **`preventDefault` 후 전체화면 진입으로 매핑**.
- **전체화면에선 컨트롤바를 숨긴다.** 마우스가 조금만 움직여도 떴다 사라져 거슬린다는
  피드백을 받았다. 화면 맨 아래(80px)로 커서를 내릴 때만 잠깐 보이게.
- **모바일**: 탭=다음은 되지만 뒤로 갈 방법이 없다 → 스와이프 필요. 길게 누르기가
  `contextmenu`를 발생시켜 의도치 않게 뒤로 간다 → 터치에서는 막을 것.
  세로는 16:9라 화면의 1/4만 쓴다 → 회전 안내. iPhone Safari는 전체화면 API가 없다 → 버튼 숨김.
- 폰트·이미지가 CDN에 있으면 현장 네트워크에서 깨진다. **손글씨 등 장식 폰트는
  쓰는 글자만 서브셋해서 내장**하면 7KB 수준이다:
  `curl "https://fonts.googleapis.com/css2?family=Nanum+Pen+Script&text=<글자들>"`

## 6. 차트를 넣을 때

`dataviz` 스킬을 먼저 읽는다. 이 덱에서 실제로 적용한 결론:

- **헤드라인 수치는 차트로 만들지 않는다.** "80명 지원 → 50명 선발, 경쟁률 1.6:1"은
  큰 숫자로 보여주는 게 맞다.
- **카테고리가 많으면(11개) 단색 가로 막대.** 색으로 구분하려 하면 못 읽는다.
  길이가 크기를 나타내고 색은 정보를 담지 않으므로 한 가지 색이면 된다.
- **막대마다 값을 직접 표기**하고, 길이는 그 차트 안 최댓값 기준으로 잡는다.
- 색은 배경 대비 3:1을 넘겨야 한다. 이 덱에선 `#006FFF`는 통과, `#22A7F5`는 2.58로 탈락.
  검증기: `node scripts/validate_palette.js "<hex,…>" --mode light` (dataviz 스킬 안).

## 7. 덱 구조 관례

- 캔버스 1920×1080, **좌우 여백 130px**, 제목 58~60px, 부제 25~26px, 본문 22~24px.
- 팔레트: `#006FFF`(메인) · `#22A7F5`(1일차) · `#0050BB`(2~3일차) · `#16305E`(4일차) ·
  `#0E1116`(어두운 장표). 허용/금지를 나타낼 때만 신호등색을 예외로 썼다.
- 블록마다 **어두운 간지 장표**를 두고 01~05 인디케이터로 현재 위치를 표시.
- 본문 장표는 `eyebrow(영문 라벨) → 제목 → 부제 → 콘텐츠 → 좌하단 푸터` 구조.
- 반복 사용 장표(Break Time / 야간 전환 / 일차 종료 / Wrap-Up 설문)는 덱 끝에 모아두고
  일차마다 숫자만 바꿔 쓴다.
- `data-label`(한글, 검색·이동용) · `data-speaker-notes`(N키)를 모든 장표에 채운다.
  `data-screen-label`은 쓰이지 않는 잔여 속성이지만 순서 변경 시 함께 재번호해 둔다.
- **README를 항상 최신으로 유지한다** — 장표 지도, 확정 수치(일정·팀·평가 배점 등),
  편집 시 주의사항. 노트북을 바꿔가며 작업하므로 이 파일이 인수인계 문서가 된다.
  장표 지도는 손으로 적지 말고 `data-label`에서 생성할 것(한 번 손으로 적었다가 틀렸다).
